@using DIViews.Models
@model DIViews.Models.Profile
@inject ProfileOptionsService Options

@using (Html.BeginForm("Save", "Home"))
{
    <h1>Update Profile</h1>
    <div class="form-group">
        <label asp-for="Name" class="control-label"></label>
        @Html.TextBoxFor(m => m.Name)
    </div>
    <div class="form-group">
        <label asp-for="Gender" class="control-label"></label>
        @Html.DropDownList("Gender",
         Options.ListGenders().Select(m =>
            new SelectListItem() { Text = m, Value = m }), "Select Gender")

    </div>
    @*<div class="form-group">
        <label asp-for="State" class="control-label"></label>
        @Html.DropDownListFor(m => m.State.Code,
            Options.ListStates().Select(s =>
                 new SelectListItem() { Text = s.Name, Value = s.Code }))
        <br />
        </div>*@
        <div class="form-group">
            <label asp-for="FavColor" class="control-label"></label>
            @Html.DropDownList("FavColor",
             Options.ListColors().Select(c =>
                new SelectListItem() { Text = c, Value = c }), "Select Colour")
        </div>
        <div class="form-group">
            <input type="submit" value="SEND" class="btn btn-outline-primary" />
        </div>
        }
